<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.oauth.server3.dao.ClientDetailsDao">

    <select id="selectByClientId" resultType="com.learn.oauth.server3.entity.CustomerClientDetails">
        SELECT * FROM  client_info WHERE client_id=#{clientId}
    </select>
</mapper>