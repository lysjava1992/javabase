<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.spring.ssm.dao.CustomerDao">
    <select id="findById" resultType="com.learn.spring.ssm.entity.Customer">
        SELECT * FROM  user WHERE id=#{id}
    </select>

    <select id="findAll" resultType="com.learn.spring.ssm.entity.Customer">
        SELECT * FROM  user
    </select>


</mapper>