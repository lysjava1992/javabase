server:
  port: 8005
spring:
  application:
    name: nacos-discovery-feign
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:

        server-addr: 127.0.0.1:8848
        # nacos 中默认 Data ID=${spring.application.name}.properties，Group=DEFAULT_GROUP
        # nacos 默认的配置文件后缀为 .properties
        file-extension: yml